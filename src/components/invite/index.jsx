import React from 'react';
import Envelope from "components/envelope";

const Invite = props => {
    return (
        <div className="wrapper invitation">
            <Envelope/>
        </div>
    );
};

Invite.propTypes = {};

export default Invite;
